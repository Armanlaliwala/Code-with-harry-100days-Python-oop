class person():
    def __init__(self):
        self.name = "Arman" #this are the public variable
        self.id = 1         #this are the public variable
A = person ()       
print(A.name) # this can be access directly
print(A.id)   # this can be access directly